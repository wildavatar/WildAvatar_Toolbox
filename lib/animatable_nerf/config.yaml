task: 'deform'
gpus: [0]

parent_cfg: 'configs/aligned_nerf_pdf_yt/base.yaml'

train_dataset:
    data_root: 'data/youtube-v2'
    human: '2WVreEPoLlg'
    ann_file: metadata.json
    split: 'train'

test_dataset:
    data_root: 'data/youtube-v2'
    human: '2WVreEPoLlg'
    ann_file: metadata.json
    split: 'test'

test:
    frame_sampler_interval: 30

# data options
# H: 1002
# W: 1000
ratio: 0.5
training_view: [0, 6, 12, 18]
init_aninerf: 'no_pretrain'
num_train_frame: 10
num_eval_frame: 20
begin_ith_frame: 0
frame_interval: 2
smpl: 'new_smpl'
vertices: 'new_vertices'
params: 'new_params'
use_bigpose: True
big_box: True
box_padding: 0.05
